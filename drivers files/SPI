void spi_init()
{
    DDRB |= (1 << DDB3) | (1 << DDB5);
    DDRB &= ~(1 << DDB4);
    SPCR = (1 << SPE) | (1 << MSTR) | (1 << SPR0);
}

uint8_t spi_transfer(uint8_t data)
{
    SPDR = data;
    while (!(SPSR & (1 << SPIF)))
        ;
    return SPDR;
}